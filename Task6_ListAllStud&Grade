SELECT s.student_id, s.first_name, s.last_name, a.assignment_name, g.score
FROM Students s
JOIN Grades g ON s.student_id = g.student_id
JOIN Assignments a ON g.course_id = a.course_id
WHERE g.course_id = 14216; -- Change the course_id to the desired course
